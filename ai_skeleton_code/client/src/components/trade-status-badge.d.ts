import type { TradeStatus } from "@shared/schema";
interface TradeStatusBadgeProps {
    status: TradeStatus;
}
export declare function TradeStatusBadge({ status }: TradeStatusBadgeProps): import("react").JSX.Element;
export {};
